# OMUS Dashboard - Environment Variables Template
# Copy to .env and fill in your values
#
# Development: docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d --build
# Production:  docker compose up -d

# ===========================================
# DATABASE
# ===========================================
MYSQL_ROOT_PASSWORD=your_secure_password_here
MYSQL_DATABASE=omus

# ===========================================
# API BASE URL
# ===========================================
API_URL=http://localhost:8080

# ===========================================
# JWT AUTHENTICATION (min 32 characters)
# ===========================================
JWT_KEY=YourSuperSecretKeyThatIsAtLeast32CharactersLong!

# ===========================================
# API KEYS
# ===========================================
API_KEY=your_api_key_here
TEXTIT_TOKEN=

# ===========================================
# ADMIN CREDENTIALS (password auto-hashed on startup)
# ===========================================
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_this_password

# ===========================================
# CORS
# ===========================================
# Production origins (comma-separated)
CORS_ORIGINS=https://omus.tmt.gob.pe
# Allow any localhost port (set true for development)
CORS_ALLOW_LOCALHOST=false

# ===========================================
# PROXY WHITELIST (comma-separated domains)
# ===========================================
PROXY_ALLOWED_DOMAINS=textit.com
