services:
  app:
    image: nginx:1.15-alpine
    container_name: omus-dashboard
    volumes:
      - ./nginx:/etc/nginx/conf.d
      - ./build/web:/var/www/app
    restart: unless-stopped
    # ports:
    #   - 3000:80

networks:
  default:
    external: true
    name: trufi-server
